# Базовые зависимости Django и PostgreSQL (уже должны быть установлены)
Django==4.2  # или более новая версия
psycopg2-binary  # Адаптер PostgreSQL для Python

# Шаблонизация и статика
django-bootstrap5  # Упрощает работу с Bootstrap 5 (не обязательно, но удобно)
# django-crispy-forms  # Упрощает создание форм (альтернатива Bootstrap)
# Эти пакеты могут быть альтернативными, выбери один.

# Разное
python-dotenv  # Загрузка переменных окружения из .env файла
Pillow  # Для обработки изображений (важно для загрузки изображений товаров и т.д.)
# bleach  # Для очистки HTML-контента (например, в блоге)
# markdown  # Для преобразования Markdown в HTML (для блогов, если нужно)

#  Для работы с почтой (если нужна отправка писем)
# django-templated-mail  # Удобная отправка email с шаблонами
# django-allauth #  Авторизация, регистрация (если нужно, альтернатива стандартной Django)

#  Для работы с админкой
django-admin-sortable2  # для сортировки объектов в админке
# django-ckeditor #  Визуальный редактор для текста в админке (например, для блога)

#  Если хочешь использовать Celery (асинхронные задачи)
# celery  # Основной пакет Celery
# redis  # Брокер сообщений для Celery (если используется Redis)
# django-celery-beat  # Планировщик задач для Celery (если нужно)

#  Для тестирования (хорошая практика)
# pytest  # Фреймворк для тестирования Python
# pytest-django  # Плагин для pytest, который упрощает тестирование Django
# factory-boy  # Для создания тестовых данных

#  Для развертывания (production, не обязательно для локальной разработки)
# gunicorn  # WSGI сервер (для запуска Django в production)

#  Полезные инструменты для разработки (не обязательно, но удобно)
# django-debug-toolbar  # Отладка в браузере (полезно для выявления проблем)
# black  # Автоматическое форматирование кода (для поддержания единообразия)
# flake8  # Проверка качества кода (для поиска ошибок и соответствия стилю)
